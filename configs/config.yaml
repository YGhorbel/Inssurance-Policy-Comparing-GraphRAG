minio:
  endpoint: "localhost:9000"
  access_key: "minioadmin"
  secret_key: "minioadmin"
  bucket_name: "regulations"
  secure: false

neo4j:
  uri: "bolt://localhost:7687"
  user: "neo4j"
  password: "password"

models:
  hf_token: "" # Set via env var HF_TOKEN usually
  model_id: "LiquidAI/LFM2-2.6B-Exp"

processing:
  chunk_size: 800
  chunk_overlap: 150

embeddings:
  model_id: "BAAI/bge-m3"
  dim: 1024
  sparse_enabled: true
  max_length: 1024
  batch_size: 8

qdrant:
  url: "http://localhost:6333"
  collection: "regulations_chunks_v2"
  # legacy_collection: retained for rollback only; do not write to it.
  legacy_collection: "regulations_chunks"
